# =====================================================================
# TransitChange Frontend — Environment Variables
# =====================================================================
# In production (e.g. Vercel), set this variable in your project settings.
# Note: Vite bakes VITE_* variables into the bundle at build time.

VITE_API_URL=http://localhost:5000/api
